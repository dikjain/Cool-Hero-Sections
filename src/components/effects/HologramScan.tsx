import React from 'react';

export const HologramScan = () => {
  return (
    <div className="absolute inset-0 overflow-hidden">
      <div className="absolute inset-0 animate-hologram-scan bg-gradient-to-b from-transparent via-cyan-500/20 to-transparent" />
    </div>
  );
};